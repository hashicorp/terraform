kind: BUG FIXES
body: '`terraform plan -detailed-exitcode` now correctly returns exit code 0 when the plan shows "No changes" but an output expression changed in a way that produces a compatible but distinct type (e.g., `list(string)` vs `tuple([string])`) with an identical value.'
time: 2026-03-16T00:00:00.000000+00:00
custom:
    Issue: "38097"
