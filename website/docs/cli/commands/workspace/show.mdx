---
page_title: 'Command: workspace show'
description: The mnptu workspace show command is used to output the current workspace.
---

# Command: workspace show

The `mnptu workspace show` command is used to output the current workspace.

## Usage

Usage: `mnptu workspace show`

The command will display the current workspace.

## Example

```
$ mnptu workspace show
development
```
